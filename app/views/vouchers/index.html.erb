<p id="notice"><%= notice %></p>

<h1>Vouchers</h1>

<% @vouchers.each do |voucher| %>
  <%= render 'shared/voucher_card', voucher: voucher %>
<% end %>

<table>
  <thead>
    <tr>
      <th>Restaurant</th>
      <th>Name</th>
      <th>Price</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vouchers.each do |voucher| %>
      <tr>
        <td><%= voucher.get_restaurant.name %></td>
        <td><%= voucher.name %></td>
        <td>£<%= voucher.price %></td>
        <td><%= voucher.notes %></td>
        <td><%= link_to 'Contact owner', voucher %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Voucher', new_voucher_path %>
